import { GroupSortExercise } from './base/GroupSortExercise';
import type { LearnExerciseProps } from './types';

export function GroupWordsTopicExercise(props: LearnExerciseProps) {
  return <GroupSortExercise {...props} />;
}
